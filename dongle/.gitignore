build*/

# Host test binaries. The suite builds in place beside its source, and .exe is
# the MinGW-w64 form on this machine.
tests/**/test_protocol
tests/**/test_protocol.exe
tests/**/test_provisioning
tests/**/test_provisioning.exe
tests/**/test_rframe
tests/**/test_rframe.exe

# provision.py's output: generated provisioning_data.h headers and the
# manifest carry a real set_key in the clear — manufacturing-floor material,
# never committed (common/provisioning.h's header comment, RADIO_PROTOCOL.md
# §10.4).
tools/out/
